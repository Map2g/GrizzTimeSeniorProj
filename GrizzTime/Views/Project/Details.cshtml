@model GrizzTime.ViewModels.Project

@*We get the employee list and contract list from here*@
@using GrizzTime.ViewModels

@{
    ViewBag.Title = "Project Details";
    int pid = Model.ProjID;

    //Employees should not be able to see buttons
    string currentUserID = Request.Cookies["UserID"].Value;
    bool isVisible;
    if ( (currentUserID == Model.BusID.ToString()) | (currentUserID == Model.ProjManID))
    {
        isVisible = true;
    }
    else
    {
        isVisible = false;
    }

}

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <!-- Tell the browser to be responsive to screen width -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="../../assets/plugins/fontawesome-free/css/all.min.css">
    <!-- Ionicons -->
    <link rel="stylesheet" href="https://code.ionicframework.com/ionicons/2.0.1/css/ionicons.min.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="../../assets/css/AdminLTE.min.css">
    @*<link rel="stylesheet" href="../../stylesheet.css">*@
    <!-- Google Font: Source Sans Pro -->
    <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700" rel="stylesheet">
</head>

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <ol class="breadcrumb float-sm-right">
                <li class="breadcrumb-item"><a href="@Request.UrlReferrer">Back</a></li>
                <li class="breadcrumb-item active">Project Details</li>
            </ol>
        </div>
    </div>
    <!-- /.container-fluid -->
</section>

<div class="container-fluid">
    <!-- Default box -->
    <div class="card">
        <div class="card-body">
            <div class="row">

                <div class="col-12 col-md-12 col-lg-8 order-2 order-md-1">
                    <div class="row">
                        <div class="col-12 col-sm-4">
                            <div class="info-box bg-light">
                                <div class="info-box-content" style="margin-left: 0rem">
                                    <span class="info-box-text text-center text-muted">Contract Allotted Hours</span>
                                    <span class="info-box-number text-center text-muted mb-0">@Model.Contract.ConAllottedHours</span>
                                </div>
                            </div>
                        </div>
                        <div class="col-12 col-sm-4">
                            <div class="info-box bg-light">
                                <div class="info-box-content" style="margin-left:0rem">
                                    <span class="info-box-text text-center text-muted">Contract Hours Remaining</span>
                                    <span class="info-box-number text-center text-muted mb-0">@Model.Contract.ConHoursRemaining</span>
                                </div>
                            </div>
                        </div>
                        <div class="col-12 col-sm-4">
                            <div class="info-box bg-light">
                                <div class="info-box-content" style="margin-left:0rem">
                                    <span class="info-box-text text-center text-muted">Project Total Hours</span>
                                    <span class="info-box-number text-center text-muted mb-0">Placeholder <span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12">
                            <div class="info-box bg-light">
                                <div class="info-box-content" style="margin-left: 20px">
                                    <h4>Project Members</h4>
                                    @foreach (var item in Project.GetEmployees(Model.ProjID))
                                    {
                                        <div class="post">
                                            <div class="user-block">
                                                <img class="img-circle img-bordered-sm" src="../../assets/img/defaultuser.jpg" alt="user image">
                                                <span class="username">
                                                    <a href="@Url.Action("Details", "Employee", new {id = item.UserID })">@item.EmpFName @item.EmpLName</a>
                                                </span>
                                                <span class="description">@item.EmpType</span>
                                            </div>
                                            <!-- /.user-block -->
                                            <p class="text-right">
                                                <a href="@Url.Action("RemoveEmpFromProject", "Project", new {pid = Model.ProjID, eid = item.UserID})" class="btn btn-sm btn-danger" style="@(isVisible? "display:inline" : "display:none")">Remove</a>
                                            </p>
                                        </div>
                                    }
                                    <div class="text-left mt-5 mb-3">
                                        <a href="@Url.Action("AddEmpToProject", "Project", new {id = Model.ProjID})" class="btn btn-sm btn-primary" style="@(isVisible? "display:inline" : "display:none")">Add Employee</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>

                <div class="col-12 col-md-12 col-lg-4 order-1 order-md-2">
                    <div class="info-box bg-light">
                        <div class="info-box-content" style="margin-left:0rem">
                            <h3 class="text-primary"><i class="fas fa-paint-brush"></i> @Html.DisplayFor(m => m.ProjName) - <b class="accent-green">@Html.DisplayFor(m => m.ProjStatus)</b> </h3>
                            <p class="text-muted"> @Html.DisplayFor(m => m.ProjDesc)</p>
                            <br>
                            <div class="text-muted">
                                <p class="text-md">
                                    Belongs to <br>
                                    <b class="d-block">@Html.DisplayFor(m => m.ContractName) Contract</b>
                                </p>
                                <p class="text-md">
                                    Project Manager<br>
                                    <b class="d-block">@Html.DisplayFor(m => m.ProjManName)</b>
                                </p>
                                <hr>
                                <p class="text-md">
                                    Start Date
                                    <b class="d-block">@Html.DisplayFor(m => m.ProjStartDate)</b>
                                </p>
                                <p class="text-md">
                                    End Date
                                    <b class="d-block">@if (Model.ProjEndDate != null) { Html.DisplayFor(m => m.ProjEndDate); } else { Html.Display("~"); }</b>
                                </p>
                            </div>

                            <hr>

                            <h5 class="mt-5 text-muted"><i class="fas fa-tasks"></i> <strong>Project Tasks</strong> </h5>
                            <table class="table table-responsive-sm">
                                <tbody>
                                    @foreach (var item in Project.GetTasks(Model.ProjID))
                                    {
                                        <tr>
                                            <td><i class="far fa-circle"></i> @item.TaskName</td>
                                            <td>@item.BillableRate/hour</td>
                                            <td style="@(isVisible? "display:inline" : "display:none")"><a href="@Url.Action("EditTask", "Project", new {id = item.TaskID})" class="text-right mt-5 mb-3">Edit Task</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <div class="text-right mt-5 mb-3">
                                <a href="@Url.Action("Edit", "Project", new {id = Model.ProjID})" class="btn btn-sm btn-primary" style="@(isVisible? "display:inline" : "display:none")">Edit Project</a>
                                <a href="@Url.Action("AddTask", "Project", new {id = Model.ProjID})" class="btn btn-sm btn-primary" style="@(isVisible? "display:inline" : "display:none")">Add Task</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- /.card-body -->
    </div>
    <!-- /.card -->
</div>


@*Displays alert message telling user how the operation went.*@
@if (@TempData["message"] != null)
{
    <script type="text/javascript">
        window.onload = function () {
            alert("@TempData["message"]");
        };
    </script>
}

